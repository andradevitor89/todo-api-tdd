## Building images to Docker Compose

```sh

docker-compose build

```

## Running application in Docker Compose

```sh

docker-compose up -d

```

## Running tests in Docker Compose

```sh

docker-compose run --rm api sh -c "npm run test"

```
